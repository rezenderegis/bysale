package com.sale.cadastro;

import android.app.Activity;

public class TableActivity extends Activity {
	/*
	 * @Override protected void onCreate(Bundle savedInstanceState) { // TODO
	 * Auto-generated method stub super.onCreate(savedInstanceState);
	 * 
	 * SaleDAO SaleDAO = new SaleDAO(this);
	 * 
	 * Intent intent = getIntent(); Pessoa cliente = (Pessoa)
	 * intent.getSerializableExtra("alunoSelecionado");
	 * 
	 * TableLayout t1;
	 * 
	 * TableLayout tl = (TableLayout) findViewById(R.id.main_table);
	 * 
	 * TableRow tr_head = new TableRow(this); tr_head.setId(10);
	 * tr_head.setBackgroundColor(Color.GRAY); tr_head.setLayoutParams(new
	 * LayoutParams( LayoutParams.FILL_PARENT, LayoutParams.WRAP_CONTENT));
	 * 
	 * 
	 * TextView label_date = new TextView(this); label_date.setId(20);
	 * label_date.setText("DATE"); label_date.setTextColor(Color.WHITE);
	 * label_date.setPadding(5, 5, 5, 5); tr_head.addView(label_date);// add the
	 * column to the table row here
	 * 
	 * TextView label_weight_kg = new TextView(this);
	 * label_weight_kg.setId(21);// define id that must be unique
	 * label_weight_kg.setText("Wt(Kg.)"); // set the text for the header
	 * label_weight_kg.setTextColor(Color.WHITE); // set the color
	 * label_weight_kg.setPadding(5, 5, 5, 5); // set the padding (if required)
	 * tr_head.addView(label_weight_kg); // add the column to the table row here
	 * 
	 * 
	 * tl.addView(tr_head, new TableLayout.LayoutParams(
	 * LayoutParams.FILL_PARENT, LayoutParams.WRAP_CONTENT));
	 * 
	 * 
	 * SaleDAO.gerarTabela(cliente);
	 * 
	 * 
	 * }
	 */
}
